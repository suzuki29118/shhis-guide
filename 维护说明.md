# SHHis 剧情整理网页维护说明

这份说明按“完全没有相关知识也能操作”的方式写。你以后更新网页时，主要只需要通过网页编辑器导出两个数据文件，再运行发布脚本，不再需要手动修改那个很大的 `index.html`。

## 现在的文件结构

```text
源码/
├─ index.html                         # 网页程序和样式
├─ data/
│  ├─ mode-a.json                     # “剧情简要整理”的内容数据
│  └─ mode-b.json                     # “相关拓展剧情”的内容数据
├─ publish.ps1                        # 一键提交并推送 GitHub 的脚本
├─ 发布到GitHub.ps1                    # 中文入口，会调用 publish.ps1
└─ 维护说明.md                         # 本说明
```

`index.html` 现在负责显示和编辑，`data/mode-a.json`、`data/mode-b.json` 负责保存内容。

## 先说明一个关键点

`https://210322.xyz/?edit=1` 只有在新版 `index.html` 和 `data` 文件夹已经上传到 GitHub 之后才会生效。

如果 GitHub 仓库里仍然只有旧版 `index.html`，线上网站就还是旧逻辑，打开 `?edit=1` 也不能编辑。这不是网址参数的问题，而是线上还没有部署新版文件。

## 以后最常用的更新流程

### 1. 打开编辑模式

打开这个地址：

```text
https://210322.xyz/?edit=1
```

如果你想强制从网站当前数据重新开始编辑，打开：

```text
https://210322.xyz/?edit=1&reset=1
```

说明：

- 普通访问 `https://210322.xyz/` 是访客只读模式。
- 加上 `?edit=1` 后，会进入编辑模式。
- 你在编辑模式里做的修改会先保存在当前浏览器里，不会立刻改动线上网站。

### 2. 在网页里修改内容

你可以像以前一样：

- 直接点文字修改标题、日期、简介。
- 拖动卡片调整顺序。
- 上传封面图。
- 用按钮复制、删除、上下移动卡片。
- 切换“剧情简要整理 / 拓展资料库”两个模式。

页面会自动保存到浏览器本地缓存。

### 3. 导出新的网站数据文件

改完以后，点击右侧工具栏里的：

```text
📦 导出网站数据文件
```

浏览器会下载两个文件：

```text
mode-a.json
mode-b.json
```

这两个就是新版网页内容。

### 4. 一键发布到 GitHub

进入本地仓库文件夹：

```text
D:\csy\video making\SHHis剧情整理\源码\shhis-guide-repo
```

在空白处右键，选择“在终端中打开”或打开 PowerShell，然后运行：

```powershell
.\publish.ps1
```

这个脚本会自动做这些事：

- 从你的下载目录里找到 `mode-a.json` 和 `mode-b.json`。
- 复制到项目的 `data` 文件夹。
- 检查 JSON 文件有没有坏。
- 提交到 Git。
- 推送到 GitHub。

如果第一次推送时弹出 GitHub 登录窗口，按提示登录即可。

你也可以运行中文入口：

```powershell
.\发布到GitHub.ps1
```

提交后，GitHub Pages 会自动更新网站。通常等几十秒到几分钟，再打开 `https://210322.xyz/` 检查。

## 本地预览方式

不要直接双击打开 `index.html`，因为浏览器可能不允许本地 HTML 读取 `data/*.json`。

推荐在这个文件夹里启动一个本地服务器：

```powershell
python -m http.server 8000
```

如果提示找不到 `python`，可以试：

```powershell
py -m http.server 8000
```

如果还是不行，这台电脑当前 Codex 自带的 Python 路径可以这样用：

```powershell
& "C:\Users\65194\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe" -m http.server 8000
```

然后用浏览器打开：

```text
http://localhost:8000/
```

本地编辑模式：

```text
http://localhost:8000/?edit=1&reset=1
```

## 两个导出按钮的区别

`💾 导出数据`：

导出一个备份文件 `SHHis_Backup.json`。它适合自己留档，或以后用“导入数据”恢复。

`📦 导出网站数据文件`：

导出网站真正使用的 `mode-a.json` 和 `mode-b.json`。以后更新 GitHub Pages，主要用这个按钮。

## 如果改乱了怎么办

### 只想撤销浏览器里的临时编辑

打开：

```text
https://210322.xyz/?edit=1&reset=1
```

它会重新读取网站当前的 `data` 文件，覆盖浏览器里的临时缓存。

## 以后推荐记住的一句话

平时只改内容时，用网页编辑器导出数据，再运行 `发布到GitHub.ps1`；只有要改网页样式或功能时，才需要改 `index.html`。
